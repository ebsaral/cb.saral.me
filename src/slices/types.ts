export type PrismicDocument = {
    id: string,
    type: string,
    tags: string[],
    lang: string,
    slug: string,
    uid: string,
    url: string,
    link_type: string,
    first_publication_date: string,
    last_publication_date: string,
    key: string,
    isBroken: boolean
}